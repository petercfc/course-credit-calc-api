type Student {
  id: ID! @unique
  name: String
  coursesPassed: [Course]
  enrolledDegree:Degree
}

type Subject {
  id: ID! @unique
  name: String!
  description: String!
}

type Degree {
  id: ID! @unique
  name: String!
  description: String!
  requiredCredits: Int!
  requiredCourses: [Course]
  department: Department
}

type Department {
  id: ID! @unique
  name: String!
  description: String!
}

type Course {
  id: ID! @unique
  name: String!
  description: String!
  number:String!
  level: Int!
  credits: Int!
  subject: Subject
  degree: Degree
  department: Department
  prerequisite:Course
}